template-id: downsample_measurement
dbrps:
  - db: replace_with_db
    rp: replace_with_rp
vars:
  from_db:
    type: string
    value: replace_with_db
  from_rp:
    type: string
    value: replace_with_rp
  measurement:
    type: string
    value: replace_with_measurement
